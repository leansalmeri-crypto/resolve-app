// Galería profesional RESOLVÉ v3. Bucket público existente: professional-work.
const RESOLVE_PHOTO_BUCKET='professional-work';
async function resolveLoadProfessionalPhotos(professionalId){const r=await db.from('professional_photos').select('*').eq('professional_id',professionalId).order('created_at',{ascending:false});if(r.error)return r;const rows=r.data||[];for(const x of rows){if(x.storage_path&&!/^https?:/i.test(x.storage_path)){const u=db.storage.from(RESOLVE_PHOTO_BUCKET).getPublicUrl(x.storage_path);x.public_url=u.data?.publicUrl||''}else x.public_url=x.storage_path||''}return {data:rows,error:null}}
async function resolveUploadProfessionalPhoto(file,caption){if(!session||profile?.role!=='professional')return {error:{message:'Solo los profesionales pueden subir fotos.'}};const count=await db.from('professional_photos').select('id',{count:'exact',head:true}).eq('professional_id',session.user.id);if(count.error)return count;if((count.count||0)>=3)return {error:{message:'Podés subir hasta 3 fotos de tus trabajos.'}};if(!file||!['image/jpeg','image/png','image/webp'].includes(file.type))return {error:{message:'Usá una imagen JPG, PNG o WEBP.'}};if(file.size>5*1024*1024)return {error:{message:'La imagen no puede superar 5 MB.'}};const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=session.user.id+'/'+Date.now()+'.'+ext;const up=await db.storage.from(RESOLVE_PHOTO_BUCKET).upload(path,file,{contentType:file.type,upsert:false});if(up.error)return up;const row=await db.from('professional_photos').insert({professional_id:session.user.id,storage_path:up.data.path,caption:caption||null});if(row.error)await db.storage.from(RESOLVE_PHOTO_BUCKET).remove([up.data.path]);return row}
async function resolveRefreshOwnPhotos(){const r=await resolveLoadProfessionalPhotos(session.user.id);$('ownPhotos').innerHTML=r.data?.length?r.data.map(x=>x.public_url?'<div class="item"><img src="'+safe(x.public_url)+'" alt="Trabajo realizado" style="width:100%;max-height:260px;object-fit:cover;border-radius:14px">'+(x.caption?'<p>'+safe(x.caption)+'</p>':'')+'</div>':'').join(''):'<div class="msg">Todavía no subiste fotos de tus trabajos.</div>'}
async function resolveUploadPhotoAction(){const f=$('proPhoto').files[0];const r=await resolveUploadProfessionalPhoto(f,$('photoCaption').value.trim());msg('photoMsg',r.error?r.error.message:'✓ Foto agregada a tu perfil.');if(!r.error){$('proPhoto').value='';$('photoCaption').value='';resolveRefreshOwnPhotos()}}
async function resolveRenderPublicPhotos(professionalId){const r=await resolveLoadProfessionalPhotos(professionalId);$('professionalPhotos').innerHTML=r.data?.length?r.data.map(x=>x.public_url?'<div class="item"><img src="'+safe(x.public_url)+'" alt="Trabajo del profesional" style="width:100%;max-height:280px;object-fit:cover;border-radius:14px">'+(x.caption?'<p>'+safe(x.caption)+'</p>':'')+'</div>':'').join(''):'<div class="msg">Este profesional todavía no cargó fotos.</div>'}
